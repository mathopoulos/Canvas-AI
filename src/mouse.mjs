//track current position of mouse on canvas
export function mouseClickPosition(e) {
  xMouse = e.clientX;
  yMouse = e.clientY;
}

//track current position of mouse on canvas
export function currentMouse(e) {
  xCurrentMouse = e.clientX;
  yCurrentMouse = e.clientY;
  
}